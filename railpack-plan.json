{
  "phases": {
    "install": {
      "cmds": ["npm ci"]
    },
    "build": {
      "cmds": [
        "rm -rf node_modules/.cache || true",
        "npm run build || echo 'no build step'"
      ]
    },
    "deploy": {
      "cmds": ["npm run start"]
    }
  }
}
