{
  "phases": {
    "install": {
      "cmds": ["npm ci"]
    },
    "build": {
      "cmds": ["npm run build || echo 'no build step'"]
    },
    "deploy": {
      "cmds": ["npm run start"]
    }
  }
}
